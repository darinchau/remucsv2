# Sole purpose is to reexport audio and some other really useful stuff from AutoMasher lol
# We will not use the song dataset class which is too prone to change at this point lol just load the files directly
# Ideally the rest of the code will not import from AutoMasher directly, but rather from this file
from AutoMasher.fyp import Audio
from AutoMasher.fyp.util import YouTubeURL, get_url
